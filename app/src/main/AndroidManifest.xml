<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />
    <uses-permission android:name="android.permission.CAPTURE_VIDEO_OUTPUT" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Optional Hardware Integration -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:name=".AURAApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AURA"
        android:hardwareAccelerated="true"
        android:requestLegacyExternalStorage="true"
        android:preserveLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AURA"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Class Mode Activity -->
        <activity
            android:name=".ui.student.ClassModeActivity"
            android:exported="false"
            android:theme="@style/Theme.AURA.ClassMode"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:screenOrientation="portrait" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AURA" />

        <!-- Main Class Mode Service -->
        <service
            android:name=".service.ClassModeService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|specialUse"
            android:foregroundServiceType="mediaProjection"
            android:permission="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="accessibility" />
        </service>

        <!-- MediaProjection Helper Service -->
        <service
            android:name=".service.MediaProjectionService"
            android:enabled="true"
            android:exported="false" />

        <!-- Accessibility Service for Escape Detection -->
        <service
            android:name=".service.AccessibilityMonitorService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/accessibility_service_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Background Sync Service -->
        <service
            android:name=".service.SyncService"
            android:enabled="true"
            android:exported="false" />

        <!-- Emergency Notification Service -->
        <service
            android:name=".service.EmergencyService"
            android:enabled="true"
            android:exported="false" />

        <!-- Network Monitoring Service -->
        <service
            android:name=".service.NetworkMonitorService"
            android:enabled="true"
            android:exported="false" />

        <!-- Hardware Integration Service -->
        <service
            android:name=".service.HardwareIntegrationService"
            android:enabled="true"
            android:exported="false" />

        <!-- File Provider for sharing screenshots -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Boot Receiver for auto-start -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Network State Receiver -->
        <receiver
            android:name=".receiver.NetworkStateReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
                <action android:name="android.net.wifi.STATE_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- Emergency Action Receiver -->
        <receiver
            android:name=".receiver.EmergencyReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="com.aura.app.EMERGENCY_ACTION" />
            </intent-filter>
        </receiver>

        <!-- Session Management Receiver -->
        <receiver
            android:name=".receiver.SessionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="com.aura.app.SESSION_START" />
                <action android:name="com.aura.app.SESSION_END" />
                <action android:name="com.aura.app.CLASS_MODE_ENABLE" />
                <action android:name="com.aura.app.CLASS_MODE_DISABLE" />
            </intent-filter>
        </receiver>

        <!-- WiFi Scan Receiver -->
        <receiver
            android:name=".receiver.WifiScanReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="com.aura.app.WIFI_SCAN" />
                <action android:name="com.aura.app.NETWORK_FOUND" />
            </intent-filter>
        </receiver>

        <!-- WorkManager Workers -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="shortService"
            tools:node="merge" />

        <!-- Deep Link Handling -->
        <activity
            android:name=".ui.DeepLinkHandler"
            android:exported="true"
            android:theme="@style/Theme.AURA.Transparent">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="aura" />
                <data android:scheme="https" />
                <data android:host="*.supabase.co" />
                <data android:path="/auth/callback" />
            </intent-filter>
        </activity>

        <!-- Emergency Quick Access -->
        <activity
            android:name=".ui.emergency.EmergencyActivity"
            android:exported="false"
            android:theme="@style/Theme.AURA.Emergency"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:screenOrientation="portrait" />

        <!-- Teacher Dashboard -->
        <activity
            android:name=".ui.teacher.TeacherDashboardActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AURA.Teacher" />

        <!-- Analytics Dashboard -->
        <activity
            android:name=".ui.teacher.AnalyticsActivity"
            android:exported="false"
            android:parentActivityName=".ui.teacher.TeacherDashboardActivity"
            android:theme="@style/Theme.AURA.Teacher" />

        <!-- Session Management -->
        <activity
            android:name=".ui.teacher.SessionManagementActivity"
            android:exported="false"
            android:parentActivityName=".ui.teacher.TeacherDashboardActivity"
            android:theme="@style/Theme.AURA.Teacher" />

        <!-- Student Management -->
        <activity
            android:name=".ui.teacher.StudentManagementActivity"
            android:exported="false"
            android:parentActivityName=".ui.teacher.TeacherDashboardActivity"
            android:theme="@style/Theme.AURA.Teacher" />

        <!-- Onboarding Activity -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.AURA.Onboarding"
            android:screenOrientation="portrait" />

        <!-- Permissions Request Activity -->
        <activity
            android:name=".ui.permissions.PermissionsActivity"
            android:exported="false"
            android:theme="@style/Theme.AURA"
            android:screenOrientation="portrait" />

        <!-- AURA Network Detection -->
        <activity
            android:name=".ui.network.AuraNetworkActivity"
            android:exported="false"
            android:theme="@style/Theme.AURA"
            android:screenOrientation="portrait" />

        <!-- Debug Activities (Remove in Production) -->
        <activity
            android:name=".ui.debug.DebugActivity"
            android:exported="false"
            android:label="@string/debug_activity_name"
            android:theme="@style/Theme.AURA"
            tools:ignore="ExportedActivity" />

        <!-- Analytics Service -->
        <service
            android:name=".service.AnalyticsService"
            android:enabled="true"
            android:exported="false" />

        <!-- Logging Service -->
        <service
            android:name=".service.LoggingService"
            android:enabled="true"
            android:exported="false" />

        <!-- Crash Reporting Service -->
        <service
            android:name=".service.CrashReportingService"
            android:enabled="true"
            android:exported="false" />

        <!-- Backup and Restore -->
        <meta-data
            android:name="com.google.android.backup.api_key"
            android:value="your_backup_api_key" />

        <!-- App Indexing -->
        <meta-data
            android:name="android.app.default_searchable"
            android:value=".ui.search.SearchActivity" />

        <!-- Firebase (Optional) -->
        <!--
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
        -->

    </application>

    <!-- Hardware Features (Optional) -->
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <!-- Screen Support -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <!-- OpenGL ES Support for AI Processing -->
    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="false" />

    <!-- Neural Networks API Support -->
    <uses-feature
        android:name="android.software.nn"
        android:required="false" />

</manifest>